# Selenium-JS

## Description

This project demonstrates how to create Web Application automation using Selenium with JavaScript.

## Pre-Installation

1. Ensure that **Node.js** and **npm** are installed on your computer.
2. Make sure your WebDriver is up to date with your browser version.
   - For Chrome, you can install the appropriate version of ChromeDriver with:
     ```shell
     npm install chromedriver@<version>
     ```
     Replace `<version>` with your specific version number.

## Installation

Install the project dependencies by running:

```shell
npm install
```

## Run test

To execute the tests, use:

```shell
npm run test
```
